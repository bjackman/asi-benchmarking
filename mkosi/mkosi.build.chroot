#!/bin/bash

set -eux

# If this doesn't exist it probably means your `BuildSources` hasn't been
# applied, it should be in your mkosi.local.conf.
cd /work/src/kernel

out="$DESTDIR/usr/lib/modules/$(make kernelrelease)"
mkdir -p $out
make install V=1 INSTALL_PATH=$out

ls -R $out